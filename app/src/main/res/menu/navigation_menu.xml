<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_registrasi"
            android:icon="@drawable/ic_add"
            android:title="@string/registrasiibadah"/>

        <item android:id="@+id/nav_jadwalpelayan"
            android:icon="@drawable/ic_calendar"
            android:title="@string/jadwalpelayan"/>
    </group>

    <group android:checkableBehavior="single">
            <item android:id="@+id/nav_qrcode"
                android:icon="@drawable/ic_qrcode"
                android:title="@string/scanqr"/>

            <item android:id="@+id/nav_profile"
                android:icon="@drawable/ic_profile"
                android:title="@string/profile"/>
    </group>
</menu>